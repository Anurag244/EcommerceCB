<%-include('./flash')%>
<%-include('./header.ejs')%>
<%-include('./navbar')%>

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>hello from product 1</h1>
    <% for(let item of products){ %>
    <h1><%=item.name%></h1>
    <h3><img src="<%=item.img%>"></h3>
    <span class="btn like-btn" product-id="<%=item._id%>">
        <!-- <% if(currentUser && currentUser.wishlist.includes(item._id)){%>
        <i class="fa-solid fa-heart"></i>
        <% } else { %>
        <i class="fa-regular fa-heart"></i>
        <% } %> -->
    </span>
    <p><%=item.price%></p>
    <p><%=item.desc%></p>
    <a href="/product/<%=item.id%>">show this</a>
    <a href="/product/<%=item.id%>/edit">Edit this</a>
    <form action="/product/<%=item.id%>?_method=DELETE" method="POST">
        <button>Delete</button>
    </form>
    <%}%>
    <script src="https://kit.fontawesome.com/7c2e1ad849.js" crossorigin="anonymous"></script>
    <script src="/js/common.js"></script>
</body>
</html>